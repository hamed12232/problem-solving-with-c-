#include <iostream>
#include <string>
#include <sstream>

using namespace std;

	
int main() {
	
	int x,y,max=0;
	cin >> x>>y;
	for (int i = 1; i <= x; i++) {
		if (x % i == 0 && y % i ==0) {
			if (i > max)
				max = i;
		}
	}
	cout << max;

}
