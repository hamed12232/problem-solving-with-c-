#include <iostream>
#include <string>
using namespace std;

int main() {
	int n,div,f=0;
	cin >> n;
	div = n / 2;
	for (int i = 2; i < div; i++) {
		if (n % i == 0) {
			cout << "NO" << endl;
			f = 1;
			break;
		}
	
	}
	if (f == 0) {
		cout << "YES" << endl;
	}
}
